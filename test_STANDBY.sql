--- MONITORING

select (
SELECT MAX(SEQUENCE#) LOG_ARCHIVED
FROM V$ARCHIVED_LOG
WHERE DEST_ID=1
AND ARCHIVED='YES'
) - (
SELECT MAX(SEQUENCE#) LOG_APPLIED
FROM V$ARCHIVED_LOG
WHERE DEST_ID=2
AND APPLIED='YES'
) from dual;


-----

